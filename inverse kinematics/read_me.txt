We need to get joint angles given the pose of end effector.


so first we define robot by using it's DH parameters.
Since our robot have spherical wrist we can use decoupling method to find last 3 joint angles first.
Then we can find first 3 joint angles .
